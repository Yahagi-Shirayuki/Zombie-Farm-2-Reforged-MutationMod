{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The game is plain web code that calls no Tauri APIs, so this grants only the core defaults and nothing else.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
