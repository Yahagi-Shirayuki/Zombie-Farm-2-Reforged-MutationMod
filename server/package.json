{
  "name": "zombiefarm-server",
  "private": true,
  "version": "0.2.0",
  "license": "MIT",
  "type": "module",
  "description": "Ground-truth save + async friend gifting API (Cloudflare Worker + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "npm run migrations:check && npm run catalogs:check && npm run typecheck && npm test && npm run test:integration && wrangler deploy --env production",
    "db:apply:local": "wrangler d1 execute zombiefarm-staging --local --file=./schema.sql",
    "db:apply:remote": "wrangler d1 execute zombiefarm-staging --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute zombiefarm-staging --local --file=./schema.sql && wrangler d1 execute zombiefarm-staging --local --file=./scripts/baseline-migrations.sql",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "typecheck": "tsc --noEmit",
    "migrations:check": "node scripts/check-migrations.mjs",
    "catalogs": "node scripts/gen-catalogs.mjs",
    "catalogs:check": "node scripts/gen-catalogs.mjs --check"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260716.1",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.111.0"
  }
}
